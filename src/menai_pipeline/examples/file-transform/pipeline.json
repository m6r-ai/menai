{
  "steps": [
    {
      "id": "read-input",
      "tool": "filesystem",
      "operation": "read_file",
      "path": "src/menai_pipeline/examples/file-transform/input.txt"
    },
    {
      "id": "transform",
      "tool": "menai",
      "inputs": {
        "content": "read-input"
      },
      "expression": "(let* ((lines (filter-list (lambda (s) (boolean-not (string=? s \"\"))) (string->list (dict-get inputs \"content\") \"\\n\"))) (sorted (sort-list string<? lines)) (upper (map-list string-upcase sorted)) (result (string-concat (list->string upper \"\\n\") \"\\n\"))) (dict \"result\" result))",
      "outputs": {}
    },
    {
      "id": "write-output",
      "tool": "console",
      "operation": "write_stdout",
      "value_from": "transform.result"
    }
  ]
}
